{% extends '_layout/default.html.twig' %}

{% block title %}Add Host{% endblock %}

{% block content %}
  <header class="mb-4 flex items-center justify-between">
    <h2 class="page-title">Add Host</h2>
  </header>

  <div class="flex">
    <div class="flex-grow mr-8">
      {{ form_start(addHostForm) }}
      {{ form_end(addHostForm) }}
    </div>
    <aside class="w-1/3 pt-8 text-sm font-light text-gray-500">
      <p class="mb-4">
        After adding a host, Fides will try to connect and check the network
        availability. In the next step, you will be asked to install the client
        on the host, so Fides can update its <code>sshd</code> configuration.
      </p>
    </aside>
  </div>
{% endblock %}
